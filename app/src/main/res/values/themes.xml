<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.Persona"  parent="Theme.AppCompat.NoActionBar">
        <item name="android:windowBackground">@color/color_default_background</item>
        <item name="android:textColor">@color/color_default_text</item>
        <item name="android:buttonStyle">@style/MyButtonStyle</item>
    </style>

    <style name="Theme.Persona" parent="Base.Theme.Persona" />

    <style name="MyButtonStyle" parent="Widget.AppCompat.Button">
        <item name="android:background">@drawable/drawable_background_default_button</item>
        <item name="android:textColor">@color/color_default_text</item>
        <item name="android:backgroundTint">@null</item>
        <item name="backgroundTint">@null</item>
    </style>

</resources>